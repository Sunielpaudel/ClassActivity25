#ifndef ADDRESS_HPP
#define ADDRESS_HPP

#include <string>

namespace customerNamespace {

    struct Address {
        std::string street;
        std::string city;
        int zipCode;
    };

} 

#endif ADDRESS_HPP